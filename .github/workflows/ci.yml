# Diagnostic branch only: avoid duplicating the full matrix with the generic CI job.
# The PR branch and its normal workflow are unchanged.
name: Continuous Integration (manual on full diagnostic branch)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Use Completion 5364 full original suite on this diagnostic branch.'
